import { FarmDynamicFormControlDirective } from './farm-dynamic-form-control.directive';

describe('FarmDynamicFormControlDirective', () => {
  it('should create an instance', () => {
    const directive = new FarmDynamicFormControlDirective();
    expect(directive).toBeTruthy();
  });
});
