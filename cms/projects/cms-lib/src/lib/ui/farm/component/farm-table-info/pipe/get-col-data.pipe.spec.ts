import { GetColDataPipe } from './get-col-data.pipe';

describe('GetColDataPipe', () => {
  it('create an instance', () => {
    const pipe = new GetColDataPipe();
    expect(pipe).toBeTruthy();
  });
});
