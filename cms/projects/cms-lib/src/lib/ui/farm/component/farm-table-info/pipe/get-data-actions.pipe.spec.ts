import { GetDataActionsPipe } from './get-data-actions.pipe';

describe('GetDataActionsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetDataActionsPipe();
    expect(pipe).toBeTruthy();
  });
});
