import { ColResizerDirective } from './col-resizer.directive';

describe('ColResizerDirective', () => {
  it('should create an instance', () => {
    const directive = new ColResizerDirective();
    expect(directive).toBeTruthy();
  });
});
