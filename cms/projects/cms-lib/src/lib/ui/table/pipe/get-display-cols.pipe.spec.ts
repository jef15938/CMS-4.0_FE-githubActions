import { GetDisplayColsPipe } from './get-display-cols.pipe';

describe('GetDisplayColsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetDisplayColsPipe();
    expect(pipe).toBeTruthy();
  });
});
