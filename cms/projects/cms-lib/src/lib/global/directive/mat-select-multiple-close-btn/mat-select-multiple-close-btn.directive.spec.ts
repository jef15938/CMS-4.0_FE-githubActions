import { MatSelectMultipleCloseBtnDirective } from './mat-select-multiple-close-btn.directive';

describe('MatSelectMultipleCloseBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new MatSelectMultipleCloseBtnDirective();
    expect(directive).toBeTruthy();
  });
});
