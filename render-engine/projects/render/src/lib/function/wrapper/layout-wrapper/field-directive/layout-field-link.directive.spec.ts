import { LayoutFieldLinkDirective } from './layout-field-link.directive';

describe('LayoutFieldLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutFieldLinkDirective();
    expect(directive).toBeTruthy();
  });
});
