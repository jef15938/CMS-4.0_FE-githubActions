import { LayoutFieldHtmlEditorDirective } from './layout-field-html-editor.directive';

describe('LayoutFieldHtmlEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutFieldHtmlEditorDirective();
    expect(directive).toBeTruthy();
  });
});
