import { LayoutFieldBgimgDirective } from './layout-field-bgimg.directive';

describe('LayoutFieldBgimgDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutFieldBgimgDirective();
    expect(directive).toBeTruthy();
  });
});
