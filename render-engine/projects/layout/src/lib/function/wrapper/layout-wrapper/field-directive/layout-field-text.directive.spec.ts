import { LayoutFieldTextDirective } from './layout-field-text.directive';

describe('LayoutFieldTextDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutFieldTextDirective();
    expect(directive).toBeTruthy();
  });
});
