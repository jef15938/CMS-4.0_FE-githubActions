import { LayoutFieldImgDirective } from './layout-field-img.directive';

describe('LayoutFieldImgDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutFieldImgDirective();
    expect(directive).toBeTruthy();
  });
});
